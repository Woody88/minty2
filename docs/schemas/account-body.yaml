type: object
required:
  - userId
  - name
  - username
  - password
properties:
  userId:
    type: string
    format: uuid

  name:
    type: string

  username:
    type: string
    format: email

  password:
    type: string
    format: password
